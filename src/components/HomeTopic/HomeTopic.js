import React from 'react';

const HomeTopic = (props) => {
    const { title, image, category, description, author } = props.blog;
    return (
        <div>

        </div>
    );
};

export default HomeTopic;